# ArduinoPythonSerialGUI
 
A few examples showing how to use the tkinter module in Python to build a GUI that can communicate with an Arduino over a serial port.  Matching Arduino code is also provided.